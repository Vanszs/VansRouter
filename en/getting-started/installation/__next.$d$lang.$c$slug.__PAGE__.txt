1:"$Sreact.fragment"
2:I[81956,["/VansRouter/_next/static/chunks/ff1a16fafef87110.js","/VansRouter/_next/static/chunks/3ba79d5b01800fdc.js"],"default"]
3:I[22982,["/VansRouter/_next/static/chunks/ff1a16fafef87110.js","/VansRouter/_next/static/chunks/3ba79d5b01800fdc.js"],"default"]
8:I[97367,["/VansRouter/_next/static/chunks/ff1a16fafef87110.js","/VansRouter/_next/static/chunks/803a574de9eda7ae.js"],"OutletBoundary"]
9:"$Sreact.suspense"
4:T206c,# Installation

Detailed installation guide for VansRouter with troubleshooting tips.

---

## Requirements

### System Requirements

- **Node.js**: Version 22.5.0 or higher
- **npm**: Version 10.0.0 or higher (comes with Node.js)
- **OS**: macOS, Linux, Windows (WSL recommended)
- **Disk Space**: ~200MB for installation

### Check Your Version

```bash
node --version
# Should show v22.5.x or higher

npm --version
# Should show 10.x.x or higher
```

**Don't have Node.js?** Install from [nodejs.org](https://nodejs.org/)

---

## Installation Methods

### Method 1: Global Installation (Recommended)

Install VansRouter globally to use from anywhere:

```bash
npm install -g vansrouter
```

**Start VansRouter:**

```bash
vansrouter
```

**Benefits:**
- ✅ Run from any directory
- ✅ Simple command: `vansrouter`
- ✅ Auto-updates with `npm update -g vansrouter`

### Method 2: Local Installation

Install in a specific project:

```bash
mkdir my-vansrouter
cd my-vansrouter
npm install vansrouter
```

**Start VansRouter:**

```bash
npx vansrouter
```

**Benefits:**
- ✅ Isolated per project
- ✅ Version control per project
- ✅ No global namespace pollution

### Method 3: From Source (Development)

Clone and build from GitHub:

```bash
git clone https://github.com/Vanszs/VansRouter.git
cd VansRouter
npm install
npm run build
npm start
```

**Benefits:**
- ✅ Latest development features
- ✅ Contribute to development
- ✅ Custom modifications

---

## First Run

### Start the Server

```bash
vansrouter
```

**What happens:**
1. Server starts on `http://localhost:20128` (the root redirects to `/masuk`)
2. Dashboard opens automatically in browser
3. Data directory created at `~/.9router`
4. API key generated automatically

### Dashboard Login

**Production login:**
- New installations default to `123456`; set a strong `INITIAL_PASSWORD` before public exposure.

**⚠️ Set a strong password before exposing the dashboard:**
1. Set `INITIAL_PASSWORD` in `.env` (for example, generate one with `openssl rand -base64 24`).
2. Login to the dashboard.
3. Change it from **Settings → Set/Replace Dashboard Password** if desired.

### Get Your API Key

```
Dashboard → Settings → API Keys
→ Copy your API key
→ Use in CLI tools
```

**Example API key format:**
```
9r_1234567890abcdef1234567890abcdef
```

---

## Verify Installation

### Check Server Status

```bash
curl http://localhost:20128/health
```

**Expected response:**
```json
{
  "status": "ok",
  "version": "1.0.0"
}
```

### List Available Models

```bash
curl http://localhost:20128/v1/models \
  -H "Authorization: Bearer your-api-key"
```

**Expected response:**
```json
{
  "object": "list",
  "data": [
    {
      "id": "cc/claude-opus-4-5-20251101",
      "object": "model",
      "created": 1234567890,
      "owned_by": "claude-code"
    }
  ]
}
```

### Test Chat Completion

```bash
curl http://localhost:20128/v1/chat/completions \
  -H "Authorization: Bearer your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "cc/claude-opus-4-5-20251101",
    "messages": [
      {"role": "user", "content": "Hello!"}
    ]
  }'
```

---

## Configuration

### Environment Variables

Create `.env` file or set environment variables:

```bash
# Security (REQUIRED in production)
export JWT_SECRET="your-secure-secret-change-this"
export INITIAL_PASSWORD="$(openssl rand -base64 24)"

# Storage
export DATA_DIR="$HOME/.9router"

# Server
export PORT="20128"
export NODE_ENV="production"

# Logging
export ENABLE_REQUEST_LOGS="false"
```

### Data Directory

**Default location:** `~/.9router` on macOS/Linux; `%APPDATA%\9router` on Windows.

**Contents:**
```
~/.9router/
  ├── db.json           # Database (providers, combos, usage)
  ├── api-keys.json     # API keys
  └── logs/             # Request logs (if enabled)
```

**Change location:**

```bash
export DATA_DIR="/custom/path"
vansrouter
```

### Port Configuration

**Default port:** `20128`

**Change port:**

```bash
export PORT="3000"
vansrouter
```

**Or use command line:**

```bash
vansrouter --port 3000
```

---

## Troubleshooting

### Port Already in Use

**Error:**
```
Error: listen EADDRINUSE: address already in use :::20128
```

**Solution 1: Kill existing process**

```bash
# Find process using port 20128
lsof -i :20128

# Kill process
kill -9 <PID>
```

**Solution 2: Use different port**

```bash
vansrouter --port 3000
```

### Permission Denied

**Error:**
```
Error: EACCES: permission denied, mkdir '/usr/local/lib/node_modules/vansrouter'
```

**Solution: Use sudo (not recommended) or fix npm permissions**

```bash
# Fix npm permissions (recommended)
mkdir ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc

# Then install again
npm install -g vansrouter
```

### Node.js Version Too Old

**Error:**
```
Error: The engine "node" is incompatible with this module
```

**Solution: Update Node.js**

```bash
# Using nvm (recommended)
nvm install 20
nvm use 20

# Or download from nodejs.org
```

### Dashboard Not Opening

**Issue:** Dashboard doesn't open automatically

**Solution 1: Open manually**

```
http://localhost:20128
```

**Solution 2: Check firewall**

```bash
# macOS: Allow Node.js in System Preferences → Security
# Linux: Check iptables
# Windows: Check Windows Firewall
```

### Cannot Connect to Providers

**Issue:** OAuth login fails or API key invalid

**Solution 1: Check internet connection**

```bash
ping google.com
```

**Solution 2: Check provider status**

- Claude Code: [status.anthropic.com](https://status.anthropic.com)
- OpenAI: [status.openai.com](https://status.openai.com)
- Gemini: [status.cloud.google.com](https://status.cloud.google.com)

**Solution 3: Regenerate API key**

```
Dashboard → Provider → Disconnect → Reconnect
```

### High Memory Usage

**Issue:** VansRouter using too much RAM

**Solution: Restart server**

```bash
# Stop
pkill -f vansrouter

# Start
vansrouter
```

**Or use PM2 for auto-restart:**

```bash
npm install -g pm2
pm2 start vansrouter --name vansrouter
pm2 save
```

---

## Deployment Options

### Local Development

```bash
npm install -g vansrouter
vansrouter
```

**Use case:** Personal coding, testing

### VPS/Cloud Server

```bash
# Install
npm install -g vansrouter

# Configure
export JWT_SECRET="your-secure-secret"
export INITIAL_PASSWORD="$(openssl rand -base64 24)"
export NODE_ENV="production"

# Start with PM2
npm install -g pm2
pm2 start vansrouter --name vansrouter
pm2 save
pm2 startup
```

**Use case:** Team access, remote coding

### Docker

```bash
docker pull ghcr.io/vanszs/vansrouter:X.Y.Z

docker run -d \
  -p 20128:20128 \
  -e JWT_SECRET="your-secure-secret" \
  -e INITIAL_PASSWORD="$(openssl rand -base64 24)" \
  -e DATA_DIR=/app/data \
  -v 9router-data:/app/data \
  --name vansrouter \
  ghcr.io/vanszs/vansrouter:X.Y.Z
```

**Use case:** Containerized deployment, Kubernetes

### Reverse Proxy (Nginx)

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:20128;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        
        # SSE support for streaming
        proxy_buffering off;
        proxy_read_timeout 86400;
    }
}
```

**Use case:** HTTPS, custom domain, load balancing

---

## Uninstallation

### Remove Global Installation

```bash
npm uninstall -g vansrouter
```

### Remove Data Directory

```bash
rm -rf ~/.9router
```

### Remove Configuration

```bash
# Remove environment variables from shell config
nano ~/.bashrc  # or ~/.zshrc
# Delete VansRouter-related exports
```

---

## Next Steps

- [Getting Started Guide](../getting-started.md) - Connect providers and start coding
- [Features](../features/) - Explore quota tracking, combos, deployment
- [Troubleshooting](../troubleshooting.md) - Fix common issues

---

## Need Help?

- **Website**: [VansRouter dashboard](http://localhost:20128)
- **GitHub**: [github.com/Vanszs/VansRouter](https://github.com/Vanszs/VansRouter)
- **Issues**: [github.com/Vanszs/VansRouter/issues](https://github.com/Vanszs/VansRouter/issues)
0:{"buildId":"PUpTBY-8r_zZtEM8SJOLp","rsc":["$","$1","c",{"children":[["$","$L2",null,{"headings":[{"level":2,"text":"Requirements","id":"requirements"},{"level":3,"text":"System Requirements","id":"system-requirements"},{"level":3,"text":"Check Your Version","id":"check-your-version"},{"level":2,"text":"Installation Methods","id":"installation-methods"},{"level":3,"text":"Method 1: Global Installation (Recommended)","id":"method-1-global-installation-recommended"},{"level":3,"text":"Method 2: Local Installation","id":"method-2-local-installation"},{"level":3,"text":"Method 3: From Source (Development)","id":"method-3-from-source-development"},{"level":2,"text":"First Run","id":"first-run"},{"level":3,"text":"Start the Server","id":"start-the-server"},{"level":3,"text":"Dashboard Login","id":"dashboard-login"},{"level":3,"text":"Get Your API Key","id":"get-your-api-key"},{"level":2,"text":"Verify Installation","id":"verify-installation"},{"level":3,"text":"Check Server Status","id":"check-server-status"},{"level":3,"text":"List Available Models","id":"list-available-models"},{"level":3,"text":"Test Chat Completion","id":"test-chat-completion"},{"level":2,"text":"Configuration","id":"configuration"},{"level":3,"text":"Environment Variables","id":"environment-variables"},{"level":3,"text":"Data Directory","id":"data-directory"},{"level":3,"text":"Port Configuration","id":"port-configuration"},{"level":2,"text":"Troubleshooting","id":"troubleshooting"},{"level":3,"text":"Port Already in Use","id":"port-already-in-use"},{"level":3,"text":"Permission Denied","id":"permission-denied"},{"level":3,"text":"Node.js Version Too Old","id":"nodejs-version-too-old"},{"level":3,"text":"Dashboard Not Opening","id":"dashboard-not-opening"},{"level":3,"text":"Cannot Connect to Providers","id":"cannot-connect-to-providers"},{"level":3,"text":"High Memory Usage","id":"high-memory-usage"},{"level":2,"text":"Deployment Options","id":"deployment-options"},{"level":3,"text":"Local Development","id":"local-development"},{"level":3,"text":"VPS/Cloud Server","id":"vpscloud-server"},{"level":3,"text":"Docker","id":"docker"},{"level":3,"text":"Reverse Proxy (Nginx)","id":"reverse-proxy-nginx"},{"level":2,"text":"Uninstallation","id":"uninstallation"},{"level":3,"text":"Remove Global Installation","id":"remove-global-installation"},{"level":3,"text":"Remove Data Directory","id":"remove-data-directory"},{"level":3,"text":"Remove Configuration","id":"remove-configuration"},{"level":2,"text":"Next Steps","id":"next-steps"},{"level":2,"text":"Need Help?","id":"need-help"}],"lang":"en","children":["$","$L3",null,{"content":"$4"}]}],["$L5","$L6"],"$L7"]}],"loading":null,"isPartial":false}
5:["$","script","script-0",{"src":"/VansRouter/_next/static/chunks/ff1a16fafef87110.js","async":true}]
6:["$","script","script-1",{"src":"/VansRouter/_next/static/chunks/3ba79d5b01800fdc.js","async":true}]
7:["$","$L8",null,{"children":["$","$9",null,{"name":"Next.MetadataOutlet","children":"$@a"}]}]
a:null
